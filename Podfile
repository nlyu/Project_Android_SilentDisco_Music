# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'Silent Disco' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod "SpotifyKit"
  pod 'Firebase'
  pod 'Firebase/Auth'
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'GooglePlaces'
end
